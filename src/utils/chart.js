import colors from '@/assets/style/color.less'

export const colorsChart = () => {
  return {
    facebook: colors.facebook,
    instagram: colors.instagram,
    shopee: colors.shopee,
    whatsapp: colors.whatsapp,
    zalo: colors.zalo,
    axes: colors.axes,
    tooltip: colors.tooltip,
    grid: colors.grid,
  }
}
